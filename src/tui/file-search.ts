// Retired: @-file search now runs server-side (src/agent/file-search.ts) and the
// TUI queries it over the API (GET /files). Kept as a stub to avoid stale
// imports; see src/tui/api-client.ts for the client-side call.
export {}
